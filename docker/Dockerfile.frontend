FROM node:alpine
RUN mkdir angular
COPY . /angular
RUN rm -rf /angular/.angular
RUN rm -rf /angular/node_modules
RUN cd angular && npm install -g @angular/cli
RUN cd angular && npm install --verbose
EXPOSE 4300
CMD ["npm", "run", "serve:prod"]
