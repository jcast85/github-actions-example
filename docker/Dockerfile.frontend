FROM node:18-alpine3.20 as angular

WORKDIR /app
COPY . .
RUN npm install
RUN npm run build:prod
COPY .htaccess /dist

FROM httpd:alpine3.15

WORKDIR /usr/local/apache2
COPY --from=angular /htdocs/app/dist/my-first-project .
COPY httpd.conf /conf
